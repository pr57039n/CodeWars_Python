# This code did not function because there was no return statement initially; by doing the math within the function and giving it a variable
# I have the function return the answer to that variable's math

def multiply(a, b):
    ans = a * b
    return ans
